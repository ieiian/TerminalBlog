# Git
.git
.gitignore

# Node / package manager state from the host
node_modules/
npm-debug.log*

# Local Wrangler state/cache from the host
.wrangler/

# Local/generated files that the image rebuilds itself
_worker.js

# Runtime/data folders
Markdown/
download/

# Editor/assistant metadata
.claude/
CLAUDE.md
INDEX.md

# Docker files not needed inside the runtime image
README.md
